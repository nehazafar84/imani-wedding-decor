const dashboardClient=window.supabase.createClient('https://lgdhudhsorazcjhtisrs.supabase.co','sb_publishable_-sZ0I8Ymjtc67J23oUyMhw_EnqXUyFm');function esc(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}function dateKey(d=new Date()){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function plusDays(n){const d=new Date();d.setDate(d.getDate()+n);return dateKey(d)}function prettyDate(v){return new Intl.DateTimeFormat('en-GB',{weekday:'short',day:'numeric',month:'short'}).format(new Date(`${v}T12:00:00`))}function money(v){return new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',minimumFractionDigits:0,maximumFractionDigits:2}).format(Number(v)||0)}function eventRow(b,showDate=true){return`<a class="dashboard-row" href="admin-bookings.html?booking=${esc(b.id)}"><div><strong>${esc(b.event_type||'Event')} · ${esc(b.customer_name)}</strong><small>${showDate?`${esc(prettyDate(b.event_date))} · `:''}${esc(b.venue||'Venue not set')}</small></div><span class="status-pill">${esc((b.status||'confirmed').replace('_',' '))}</span></a>`}async function loadDashboard(){const{data:{session}}=await dashboardClient.auth.getSession();if(!session){location.href='admin-login.html';return}const today=dateKey(),future=plusDays(30);const[bookingResult,inventoryResult]=await Promise.all([dashboardClient.from('bookings').select('id,customer_name,event_type,event_date,venue,status,van_assignment,total_amount,paid_amount').not('status','in','("archived")').order('event_date'),dashboardClient.from('inventory_items').select('id,name,category,storage_1_quantity,storage_2_quantity,is_active').eq('is_active',true).order('name')]);if(bookingResult.error||inventoryResult.error){document.querySelectorAll('.dashboard-list').forEach(el=>el.innerHTML='<div class="admin-empty">Could not load dashboard data. Please refresh.</div>');return}const bookings=bookingResult.data||[],inventory=inventoryResult.data||[];const todayBookings=bookings.filter(b=>b.event_date===today&&!['completed','archived'].includes(b.status));const upcoming=bookings.filter(b=>b.event_date>today&&b.event_date<=future&&!['completed','archived'].includes(b.status));const due=bookings.filter(b=>!['completed','archived'].includes(b.status)&&Number(b.total_amount)>Number(b.paid_amount)).sort((a,b)=>a.event_date.localeCompare(b.event_date));const warnings=inventory.map(i=>({...i,total:Number(i.storage_1_quantity||0)+Number(i.storage_2_quantity||0)})).filter(i=>i.total<=5).sort((a,b)=>a.total-b.total);document.getElementById('dashTodayCount').textContent=todayBookings.length;document.getElementById('dashTodayHint').textContent=todayBookings.length?'Active today':'No events today';document.getElementById('dashUpcomingCount').textContent=upcoming.length;document.getElementById('dashOutstanding').textContent=money(due.reduce((sum,b)=>sum+Math.max(Number(b.total_amount||0)-Number(b.paid_amount||0),0),0));document.getElementById('dashWarningCount').textContent=warnings.length;document.getElementById('dashTodayList').innerHTML=todayBookings.length?todayBookings.map(b=>eventRow(b,false)).join(''):'<div class="admin-empty">No active events today.</div>';document.getElementById('dashUpcomingList').innerHTML=upcoming.length?upcoming.slice(0,6).map(b=>eventRow(b,true)).join(''):'<div class="admin-empty">No events in the next 30 days.</div>';document.getElementById('dashPaymentList').innerHTML=due.length?due.slice(0,6).map(b=>`<a class="dashboard-row" href="admin-payments.html"><div><strong>${esc(b.customer_name)} · ${esc(b.event_type||'Event')}</strong><small>${esc(prettyDate(b.event_date))} · Remaining ${money(Math.max(Number(b.total_amount||0)-Number(b.paid_amount||0),0))}</small></div><span class="payment-status ${Number(b.paid_amount)>0?'partial':'unpaid'}">${Number(b.paid_amount)>0?'Part paid':'Unpaid'}</span></a>`).join(''):'<div class="admin-empty">No outstanding payments.</div>';document.getElementById('dashInventoryList').innerHTML=warnings.length?warnings.slice(0,8).map(i=>`<a class="dashboard-row" href="admin-inventory.html"><div><strong>${esc(i.name)}</strong><small>${esc(i.category||'Décor')} · S1 ${Number(i.storage_1_quantity||0)} · S2 ${Number(i.storage_2_quantity||0)}</small></div><span class="inventory-warning ${i.total===0?'zero':''}">${i.total} total</span></a>`).join(''):'<div class="admin-empty">No low-stock warnings.</div>'}document.getElementById('dashboardSignOut').onclick=async()=>{await dashboardClient.auth.signOut();location.href='admin-login.html'};loadDashboard();